import { ComingSoon } from "../_components/ComingSoon";

export default function UsersPage() {
  return <ComingSoon title="Users & roles" blurb="Who can see and approve what. Needs auth (Auth.js) wired up first — there's no sign-in yet, so there are no real users." />;
}
