import { ComingSoon } from "../_components/ComingSoon";

export default function ReportsPage() {
  return (
    <ComingSoon
      title="Reports"
      blurb="An audit pack an assurer can recompute in Excel. Blocked on the calculation pipeline — there are no ActivityRecords or EmissionRecords yet, only answered questionnaire data, so there's no emissions figure to report."
    />
  );
}
