import { ComingSoon } from "../_components/ComingSoon";

export default function ReportsPage() {
  return (
    <ComingSoon
      title="Reports"
      blurb="An audit pack an assurer can recompute in Excel. Reports are ready once your first period is fully approved — nothing's been approved yet, so there's no figure to report."
    />
  );
}
