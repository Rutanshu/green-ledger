import { ComingSoon } from "../_components/ComingSoon";

export default function DocumentsPage() {
  return <ComingSoon title="Documents" blurb="Invoices, meter reads, EAC certificates — the evidence behind an answer. Needs object storage (R2) wired up before uploads work." />;
}
