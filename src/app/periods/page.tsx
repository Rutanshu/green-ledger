import { ComingSoon } from "../_components/ComingSoon";

export default function PeriodsPage() {
  return <ComingSoon title="Periods" blurb="Reporting periods and their lock status. FY2026 exists and is DRAFT (unlocked). Locking a period and refusing edits afterwards isn't wired up yet." />;
}
