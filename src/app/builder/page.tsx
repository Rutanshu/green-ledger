import { ComingSoon } from "../_components/ComingSoon";

export default function BuilderPage() {
  return (
    <ComingSoon
      title="Questionnaire Builder"
      blurb="Where the 'Standard Operations' template — 6 sections, 13 questions — was authored. Right now it only exists because the seed script wrote it directly; nobody has built or published a template through this screen yet."
    />
  );
}
