import { ComingSoon } from "../_components/ComingSoon";

export default function SitesPage() {
  return (
    <ComingSoon
      title="Sites"
      blurb="A site profile, ownership periods, and asset list per site. The 4 demo sites are visible on the Dashboard now — this is where you'd edit them."
    />
  );
}
