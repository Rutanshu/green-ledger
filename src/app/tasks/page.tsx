import { ComingSoon } from "../_components/ComingSoon";

export default function TasksPage() {
  return <ComingSoon title="Tasks" blurb="Assigned follow-ups — an overdue answer, a broken binding to fix, a document to chase. The Task model exists in the schema; nothing writes to it yet." />;
}
