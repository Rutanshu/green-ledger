import { ComingSoon } from "../_components/ComingSoon";

export default function DataCollectionPage() {
  return (
    <ComingSoon
      title="Data Collection"
      blurb="The questionnaire a contributor actually fills in — only the questions that apply to that site, per visible_if. The demo has answers seeded directly into the database; nobody has typed them through a form yet."
    />
  );
}
