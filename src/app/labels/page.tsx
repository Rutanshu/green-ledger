import { ComingSoon } from "../_components/ComingSoon";

export default function LabelsPage() {
  return (
    <ComingSoon
      title="Labels"
      blurb="Rename any system code without changing what it means underneath — resolveLabel() and 7 seeded overrides already exist (e.g. 'Stationary combustion' → 'Fixed fuel burning'). This screen would let you edit them; right now they're only visible in the database."
    />
  );
}
