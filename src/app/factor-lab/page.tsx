import { ComingSoon } from "../_components/ComingSoon";

export default function FactorLabPage() {
  return (
    <ComingSoon
      title="Factor Lab"
      blurb="Where the 2 broken factor bindings from the Dashboard would actually get fixed — browsing factor sets, adding a missing factor, re-testing a binding. checkBindingHealth() already computes the right answer; this is the screen to act on it."
    />
  );
}
