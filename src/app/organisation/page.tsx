import { ComingSoon } from "../_components/ComingSoon";

export default function OrganisationPage() {
  return <ComingSoon title="Organisation" blurb="Consolidation approach, base year, and the org-wide settings that scope every later number. Currently set once, by the seed script." />;
}
